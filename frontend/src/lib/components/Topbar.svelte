<script lang="ts">
  import PlanBadge from './PlanBadge.svelte'; import UserMenu from './UserMenu.svelte'; import { onMount } from 'svelte'; import { currentUser } from '$lib/stores/user'; import { fetchUnreadNotificationsCount } from '$lib/api';
  let unread = 0; onMount(async()=>{try{const r:any=await fetchUnreadNotificationsCount();unread=r.count||0;}catch{}});
</script>
<div class="topbar"><a class="logo" href="/radar">Trend Scope</a><input aria-label="Recherche" placeholder="Rechercher une niche, un format, une intention" /><PlanBadge plan={$currentUser?.plan || 'free'} /><a class="notif" href="/notifications">🔔 <span>{unread}</span></a><UserMenu /></div>
<style>.topbar{position:sticky;top:0;z-index:5;display:grid;grid-template-columns:auto 1fr auto auto auto;gap:.75rem;align-items:center;padding:.8rem 1rem;border-bottom:1px solid var(--border);background:color-mix(in srgb,var(--brand-panel) 88%, transparent);backdrop-filter:blur(10px)}.logo{font-weight:800;letter-spacing:-.02em;background:var(--gradient-brand);-webkit-background-clip:text;color:transparent;background-clip:text}.notif{display:flex;gap:.4rem;padding:.55rem .7rem;border:1px solid var(--border);border-radius:10px;background:var(--surface-soft)}input{border:1px solid var(--border);background:var(--bg);color:var(--text);border-radius:10px;padding:.55rem .7rem}@media(max-width:760px){.topbar{grid-template-columns:1fr auto auto}.topbar input{grid-column:1/-1}}</style>
